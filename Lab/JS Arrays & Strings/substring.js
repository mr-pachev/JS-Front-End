function solve(argumen, startIndex, endIndex) {
  //let text = argumen.slice(startIndex, startIndex + endIndex);
  let text = argumen.substring(startIndex, startIndex + endIndex);

  console.log(text);
}

solve("ASentence", 1, 8);
