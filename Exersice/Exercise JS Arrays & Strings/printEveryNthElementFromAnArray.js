function solve(arr, n){
    let newArr = [];

    for (let i = 0; i < arr.length; (i +=Number(`${n}`))){
        let curren = arr[i];
        // newArr.push(arr[i]);
        console.log(curren);
    }
    
}

solve(['dsa',
'asd', 
'test', 
'tset'], 
2

);